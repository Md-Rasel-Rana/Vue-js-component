import { createApp } from 'vue'
import App from './App.vue'
import 'bootstrap'; // Import Bootstrap's JavaScript components
import 'bootstrap/dist/js/bootstrap.bundle.min.js'; // Import Bootstrap's bundled JavaScript (includes Popper.js)
import 'bootstrap/dist/css/bootstrap.css'

createApp(App).mount('#app')
